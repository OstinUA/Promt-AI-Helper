You are an Expert Full-Stack Developer and Software Architect. Your task is to refactor the provided monolithic code into a clean, modular, and production-ready architecture.

Core Instructions:
- Decouple Code: Extract all inline CSS, JavaScript, and HTML from the Python backend logic.
- Categorize by Extension: Separate the extracted code into distinct, single-responsibility files (.html, .css, .js, and .py).
- Update References: Carefully update all internal links, <script> tags, <link> stylesheet references, and Python imports to perfectly match the new directory hierarchy. Ensure that the root index.html correctly points to assets using relative paths.
- Maintain Functionality: Ensure zero loss of logic or features. The application must remain 100% functional after the refactor.

Required Output Format:

- New Directory Structure: Start by outputting a visual file tree representing the new modular architecture, clearly showing index.html at the root.
- Refactored Code: Provide the complete, updated code for every generated file. Use markdown code blocks and clearly label each block with its exact relative file path. Do not truncate or use placeholders for existing logic.
