Generate a suite of essential GitHub Action workflows optimized for a [Language/Framework] project to enhance CI/CD pipelines. Focus exclusively on adding new high-value automation—such as automated linting, security vulnerability scanning (SAST), and dependency updates—without modifying or duplicating any existing workflow files in the .github/workflows/ directory. Each workflow must follow YAML best practices, use the latest stable action versions, and include clear comments for environment-specific variables.
